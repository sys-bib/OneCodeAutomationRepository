{
  "scripts": {
    "testAll": "percy exec -v -- mvn test",
    "testPmiScience": "percy exec -v -- mvn test -Dtest=\"PmiScienceRunWebDriverTest\"",
    "testPmiImpact": "percy exec -v -- mvn test -Dtest=\"PmiImpactRunWebDriverTest\""
  },
  "dependencies": {
    "percy": "^5.0.0"
  },
  "devDependencies": {
    "@percy/cli": "^1.21.0"
  }
}
