{
  "scripts": {
    "test-default": "percy exec -v -- mvn test",
    "test-pmiscience.com": "percy exec -v -- mvn test -Dtest=\"PmiScienceRunWebDriverTest\"",
    "test-pmi-impact.com": "percy exec -v -- mvn test -Dtest=\"PmiImpactRunWebDriverTest\""
  },
  "dependencies": {
    "percy": "^5.0.0"
  },
  "devDependencies": {
    "@percy/cli": "^1.21.0"
  }
}
